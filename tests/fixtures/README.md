# Test fixtures

Place placeholder images here for offline smoke testing.

Rules:

- Use **only public-domain / copyright-free** images, or images generated on
  the fly with Pillow (the smoke test already generates its own placeholders,
  so this folder is optional).
- Never commit personal photos or other sensitive data.
- The smoke test (`tests/smoke_test.py`) does not depend on real photos.
